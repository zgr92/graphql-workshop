1. Get all employees whose title is "Sales Representative". Display their first and last names
2. Name the query "getAllSalesReps".
3. Create a query that will get all employees with a title passed to it as a variable.